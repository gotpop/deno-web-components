# Development environment configuration
ENVIRONMENT=development

# Server configuration
PORT=8000

# Live reload settings
LIVE_RELOAD_ENABLED=true

# File watching paths (comma-separated)
WATCH_PATHS=./public,./src

# Debug settings
DEBUG=false
