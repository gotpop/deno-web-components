{
  "name": "deno-server",
  "version": "1.0.0",
  "description": "Simple Deno HTTP server",
  "scripts": {
    "start": "deno run --allow-net --allow-read src/server.ts",
    "dev": "deno run --allow-net --allow-read --watch src/server.ts"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "deno": ">=1.0.0"
  }
}