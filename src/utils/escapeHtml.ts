/**
 * Safely converts HTML or other code into human-readable text for display as a code snippet.
 * Escapes only necessary HTML special characters.
 *
 * @param {string} str - The string to convert.
 * @returns {string} - The safely converted string.
 */
export function escapeHtml(str: string): string {
  return str
}
