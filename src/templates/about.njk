{% extends "base-hero.njk" %}

{% block hero %}
  <h1 class="heading" id="heading-primary">About page</h1>
  <p class="subheading" id="subheading">A demonstration of the latest in CSS and Web Components</p>
{% endblock %}

{% block page_content %}
  {% include "partials/page-nav/page-nav.njk" %}
  <layout-item
    start="14"
    end="31"
    >
    <surface-item variant="light" size="large">
      {% for item in bodyMarkupData %}
        {% if item.tag == "h2" %}
          <h2 variant="xl" flow="body" id="{{ item.id }}">{{ item.content }}</h2>
        {% elif item.tag == "h3" %}
          <h3 variant="lg" flow="body" id="{{ item.id }}">{{ item.content }}</h3>
        {% elif item.tag == "p" %}
          <p>{{ item.content }}</p>
        {% elif item.tag == "ul" %}
          <ul>
            {% for listItem in item.items %}
              <li>{{ listItem }}</li>
            {% endfor %}
          </ul>
        {% else %}
          <{{ item.tag }} {% if item.id %}id="{{ item.id }}"{% endif %}>{{ item.content }}</{{ item.tag }}>
        {% endif %}
      {% endfor %}
    </surface-item>
  </layout-item>
{% endblock %}