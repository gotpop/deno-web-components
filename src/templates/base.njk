<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "partials/head.njk" %}
  </head>
  <body>
    <app-shell
      aria-label="application"
      id="app-shell"
      role="application"
    >
      <header 
        aria-label="Site header"
        class="header" 
        id="header" 
        role="banner" 
      >
        <header-logo aria-label="Site logo">
          <a 
            aria-label="Home"
            class="link-logo" 
            href="{{ BASE_URL }}" 
          >
            {% include "partials/logo/logo.njk" %}
          </a>
          <button 
              aria-controls="header-nav"
              aria-expanded="false"
              aria-haspopup="true"
              aria-label="Toggle navigation"
              class="button-toggle-menu" 
              id="header-toggle" 
              type="button"
            >
             <span>☰</span>
             <span hidden>Toggle navigation</span>
            </button>
        </header-logo>
        <div
          id="main-menu-popover"
          aria-label="Primary navigation"  
          aria-orientation="horizontal"
          class="header-nav"
        >
          {% for item in navigationData.menuItems %}
            {% include "partials/menu-item.njk" %}
          {% endfor %}
        </div>
      </header>

      <main
        aria-label="Main content" 
      >
        {% block content %}{% endblock %}
      </main>

      {% include "partials/site-footer.njk" %}
    </app-shell>
    {% include "partials/feature-detect-popover/feature-detect-popover.njk" %}
  </body>
</html>