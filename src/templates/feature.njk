{% extends "base-hero.njk" %}

{% block hero %}
    <h1 class="heading" id="heading-primary">{{ currentFeature.title }}</h1>
{% endblock %}

{% block page_content %}
    <layout-item
            end="22"
            start="4"
            >
        <surface-item variant="light" size="large" aria-label="{{ currentFeature.title }}">
            <p>{{ currentFeature.description }}</p>
            <a href="/features">
                <span>Back to Features</span></a>
        </surface-item>
    </layout-item>
    <layout-item
            end="40"
            start="26"
            >
        <surface-item variant="light" size="large" aria-label="{{ currentFeature.title }}">
            <h2 variant="xl">Feature links</h2>
            {% for link in currentFeature.links %}
                <a href="{{ link.link }}">
                    <span>{{ link.label }}</span></a>
            {% endfor %}
        </surface-item>
    </layout-item>
{% endblock %}