<header-nav-item 
    {% if item.isExternal %}
      width="narrow" 
    {% endif %}
  >
  <a 
  class="menu-item-link"
  role="menuitem" 
  data-variant="{{ 'compact' if item.isExternal else 'normal' }}"
  href="{{ item.href }}"
  aria-label="{{ item.name }}"
  {{ item.key | isActive(currentPage) }}
  {% if item.isExternal %}
    target="_blank"
    rel="noopener"
  {% endif %}
  >
    {% if item.isExternal %}
      <span hidden>{{ item.name }}</span>
      <icon-github-inversed aria-hidden="true"></icon-github-inversed>
    {% else %}
      <span>{{ item.name }}</span>

    {% endif %}
  </a>
</header-nav-item>