<grid-item
  id="{{ item.id }}"
  role="region"
  aria-label="{{ item.label }}"
  data-columns="{{ item.columns }}"
  data-rows="{{ item.rows }}"
  ><{{ item.titleTag }}>{{ item.title }}</{{ item.titleTag }}>
</grid-item>
